import { EmpRankOrdinalPipe } from './emp-rank-ordinal.pipe';

describe('EmpRankOrdinalPipe', () => {
  it('create an instance', () => {
    const pipe = new EmpRankOrdinalPipe();
    expect(pipe).toBeTruthy();
  });
});
